# Python bytecode
*.pyc
__pycache__/
data/
src/outputs/
outputs/

# Python build / packaging artifacts
*.egg-info/
build/
dist/

# Sphinx build output
docs/_build/

# Environment files
.env
.env.local
.env.*.local

# SSL Certificates (keep .gitkeep)
docker/certs/*.pem
docker/certs/*.crt
docker/certs/*.cer
.DS_Store

# apptainer
*.sif

# agents
.claude/
CLAUDE.md
docs/superpowers/
src/logs.log

# MOOSE-derived artifacts (regenerated by exporter + simulation).
# The only tracked files under src/cases/alpha_d/moose/ are *.i (input
# decks) and __init__.py. All Exodus outputs, postprocessor CSVs,
# Forchheimer CSV+sidecar, and diagnostic runs are gitignored.
src/cases/alpha_d/moose/*.e
src/cases/alpha_d/moose/*.csv
src/cases/alpha_d/moose/*.json
tests/cases/claim_*/*
